import React from "react"

const initialState = {
      counter: 234
}

const reducer = (state, action) => {
  switch(action.title) {
    case("up"):
    case("down"):
  }
}

class  CounterRedux extends React.Component {

  render () {
    return (
        <h1> Counter: </h1>
    )
  }
}
